---
import Header from '../components/dashboard/Header.astro';
import Sidebar from '../components/dashboard/Sidebar.astro';
import '../styles/global.css';
import { themeInitScript } from '../utils/theme';

interface Props {
  title: string;
  description?: string;
  currentPath?: string;
}

const { title, description = 'Restaurant Front Desk Dashboard', currentPath = Astro.url.pathname } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title} · TeaTime Premium</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <script is:inline set:html={themeInitScript} />
  </head>
  <body class="bg-background text-text antialiased">
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-[100]
             focus:px-4 focus:py-2 focus:bg-primary focus:text-white focus:rounded-lg"
    >
      Skip to main content
    </a>

    <Header />

    <div class="flex max-w-[1600px] mx-auto">
      <Sidebar currentPath={currentPath} />

      <main id="main-content" class="flex-1 min-w-0 px-4 sm:px-6 lg:px-8 py-6 lg:py-8">
        <slot />
      </main>
    </div>
  </body>
</html>
