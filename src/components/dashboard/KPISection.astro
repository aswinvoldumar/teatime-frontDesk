---
import KPICard from './KPICard.astro';
import { kpiData } from '../../data/kpi';
---

<section aria-label="Key performance indicators" class="mb-6 lg:mb-8">
  <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
    {kpiData.map((kpi) => (
      <KPICard kpi={kpi} />
    ))}
  </div>
</section>
